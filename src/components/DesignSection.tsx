const DesignSection = ({ active }) => {

    //@ts-ignore
    return (<div className="section" id="setup" active={active}>
        <p>check back in 5 years</p>
    </div>);
}

export default DesignSection;